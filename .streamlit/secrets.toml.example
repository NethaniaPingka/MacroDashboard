# Copy to secrets.toml and fill in. secrets.toml is gitignored.
#
# FRED  â€” free, instant: https://fredaccount.stlouisfed.org/apikeys
# BPS   â€” free, register: https://webapi.bps.go.id/developer
#
# Environment variables of the same name take precedence over this file,
# which is how the app will read secrets once deployed.

FRED_API_KEY = "your-32-character-fred-key"
BPS_APP_KEY = "your-bps-app-key"

